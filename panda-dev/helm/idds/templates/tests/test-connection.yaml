apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idds.fullname" . }}-test-connection"
  labels:
    {{- include "idds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
