apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harvester.fullname" . }}-test-connection"
  labels:
    {{- include "harvester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harvester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
