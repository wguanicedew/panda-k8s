apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bigmon.fullname" . }}-test-connection"
  labels:
    {{- include "bigmon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bigmon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
